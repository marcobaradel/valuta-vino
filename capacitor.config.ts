import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'valuta.vino',
  appName: 'valuta-vino',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
